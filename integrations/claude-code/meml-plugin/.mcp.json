{
  "mcpServers": {
    "meml": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/meml-mcp.mjs"],
      "env": {
        "MEML_STATE_PATH": "${HOME}/.meml/state/claude-code.state",
        "MEML_ACTOR": "claude-code",
        "MEML_RECEIPT_PREFIX": "claude-verified-"
      }
    }
  }
}
