# retrieval-v1

This directory is reserved for the frozen, human-reviewed retrieval evaluation dataset.

Each release must provide a manifest, deterministic seed records, multi-label relevance annotations, and a reviewed baseline. Record identities must be stable semantic keys rather than runtime-assigned node IDs. Dataset updates require a new version directory.
